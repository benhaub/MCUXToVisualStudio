// Rt1060Startup.cpp : Defines the entry point for the application.
//

#include "Rt1060Startup.h"
#include <math.h>

int main()
{
    int i = 0;
    i = pow(1,1);
    i = sqrt(4);
    while (1) {
        i = pow(1, 1);
        i = sqrt(4);
    }

    return 0;
}
